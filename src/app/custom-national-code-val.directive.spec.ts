import { CustomNationalCodeValDirective } from './custom-national-code-val.directive';

describe('CustomNationalCodeValDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNationalCodeValDirective();
    expect(directive).toBeTruthy();
  });
});
